# Migration `20200814171724-init`

This migration has been generated by neulhan at 8/14/2020, 5:17:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `neulhanDB`.`User` (
`age` int   ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`email` varchar(191) NOT NULL  ,`id` varchar(191) NOT NULL  ,`name` varchar(191)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `neulhanDB`.`Post` (
`authorID` varchar(191) NOT NULL ,`content` varchar(191)   ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` varchar(191) NOT NULL  ,`published` boolean NOT NULL  ,`title` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `neulhanDB`.`User`(`email`)

ALTER TABLE `neulhanDB`.`Post` ADD FOREIGN KEY (`authorID`) REFERENCES `neulhanDB`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200814171724-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+datasource db {
+    provider = "mysql"
+    url      = env("DATABASE_URL")
+}
+
+generator db {
+    provider = "go run github.com/prisma/prisma-client-go"
+    output = "./db/db_gen.go"
+    package = "db"
+}
+
+model User {
+    id        String   @default(cuid()) @id
+    createdAt DateTime @default(now())
+    email     String   @unique
+    name      String?
+    age       Int?
+
+    posts     Post[]
+}
+
+model Post {
+    id        String   @default(cuid()) @id
+    createdAt DateTime @default(now())
+    updatedAt DateTime @updatedAt
+    published Boolean
+    title     String
+    content   String?
+
+    author   User @relation(fields: [authorID], references: [id])
+    authorID String
+}
```


